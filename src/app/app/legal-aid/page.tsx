import { ExternalLink, MapPin, Phone } from "lucide-react";
import { getLegalAidServices } from "@/lib/legal/aid";
import { LegalAidQuiz } from "@/components/legal-aid/legal-aid-quiz";
export const metadata = { title: "Legal help" };
export default async function LegalAidPage() { const services = await getLegalAidServices(12); return <div className="workspace-page"><header className="max-w-4xl"><p className="eyebrow text-navy-700">Official legal aid</p><h1 className="mt-3 max-w-4xl font-serif-display text-4xl leading-tight text-navy-950 sm:text-5xl">Legal help should not depend on whether you can afford it.</h1><p className="mt-4 max-w-2xl text-sm leading-6 text-ink-600">Check common eligibility criteria, then contact an official legal services authority for confirmation and representation.</p></header><div className="mt-9 grid gap-9 xl:grid-cols-[minmax(0,1fr)_23rem]"><div><p className="eyebrow mb-4">Guided eligibility check</p><LegalAidQuiz /></div><aside><p className="eyebrow mb-4">Official services</p><div className="border-t border-ink-300">{services.length ? services.map((service) => <article key={service.id} className="border-b border-ink-200 py-5"><div className="flex items-start justify-between gap-3"><h2 className="text-sm font-semibold text-navy-950">{service.name}</h2><span className="text-[9px] font-bold uppercase tracking-[0.1em] text-verified-700">Official</span></div>{service.description ? <p className="mt-2 text-xs leading-5 text-ink-500">{service.description}</p> : null}<div className="mt-3 space-y-1">{service.phone ? <a href={`tel:${service.phone}`} className="flex items-center gap-2 text-xs font-semibold text-navy-800"><Phone className="h-3.5 w-3.5" />{service.phone}</a> : null}{service.address ? <p className="flex items-start gap-2 text-xs leading-5 text-ink-500"><MapPin className="mt-0.5 h-3.5 w-3.5 shrink-0" />{service.address}</p> : null}{service.website ? <a href={service.website} target="_blank" rel="noreferrer" className="flex items-center gap-2 text-xs font-semibold text-navy-800">Official website <ExternalLink className="h-3.5 w-3.5" /></a> : null}</div></article>) : <p className="py-8 text-sm text-ink-400">No official services are listed for this location yet.</p>}</div><p className="mt-5 text-[11px] leading-5 text-ink-400">NyayAI does not provide legal representation. Eligibility and service availability are confirmed by the relevant authority.</p></aside></div></div>; }
